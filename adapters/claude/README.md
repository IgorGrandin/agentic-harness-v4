# Claude adapter

Materializes Core plus the Coder profile into `~/.claude/CLAUDE.md` and copies
the three allowlisted V4 skills to `~/.claude/skills/`. Claude Code's project
instructions, agents, MCP configuration, credentials, and model selection are
not managed by this adapter.
